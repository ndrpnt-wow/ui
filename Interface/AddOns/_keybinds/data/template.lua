local _, class = UnitClass("player")
if class ~= "???" then
	return
end

local _, ns = ...

ns.macros[000] = {

	["1"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["2"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["3"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["4"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["5"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["q"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["w"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["e"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["r"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["t"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["a"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["g"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["h"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["z"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

	["b"] = [[
/use [nomod]
/use [mod:shift, @focus]
/use [mod:alt]
/use [mod:ctrl, @focus]
]],

}
